/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com', 'res.cloudinary.com'], // Add your domain(s) here
      },
}

module.exports = nextConfig
